# Optional managed defaults for development or a secured runtime environment.
# Never commit live provider credentials or bundle them into a desktop installer.
MIRAE_DEFAULT_OPENROUTER_API_KEY=
MIRAE_DEFAULT_UNISWAP_API_KEY=
